{
  "name": "dora-exporter",
  "version": "1.0.0",
  "private": true,
  "description": "Exporte les quatre metriques DORA depuis l'API Jenkins vers Prometheus",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
