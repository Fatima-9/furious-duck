services:
  prometheus:
    image: prom/prometheus:v2.55.1
    container_name: furious_duck_prometheus_preprod
    restart: unless-stopped
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--web.external-url=https://preprod.dsp5-archi-o24a-g2.fr/prometheus"
      - "--web.route-prefix=/prometheus"
    volumes:
      - ${VM_PROJECT_DIR:-/home/thetiptop_gp2/furious-duck}/monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data_preprod:/prometheus
    networks:
      - default
      - traefik_proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik_proxy"
      - "traefik.http.routers.prometheus-preprod.rule=(Host(`preprod.dsp5-archi-o24a-g2.fr`) || Host(`preprod.dsp5-archi-o24a-g2.com`)) && (Path(`/prometheus`) || PathPrefix(`/prometheus/`))"
      - "traefik.http.routers.prometheus-preprod.priority=1000"
      - "traefik.http.routers.prometheus-preprod.entrypoints=websecure"
      - "traefik.http.routers.prometheus-preprod.tls=true"
      - "traefik.http.routers.prometheus-preprod.tls.certresolver=letsencrypt"
      - "traefik.http.services.prometheus-preprod.loadbalancer.server.port=9090"
  grafana:
    image: grafana/grafana:11.3.0
    container_name: furious_duck_grafana_preprod
    restart: unless-stopped
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      # Injecte par Jenkins depuis le credential furious-duck-grafana-admin-password.
      # Grafana reapplique cette valeur a chaque demarrage : la laisser en dur
      # dans le depot ecrasait le mot de passe reel a chaque deploiement.
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:?GRAFANA_ADMIN_PASSWORD est requis}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=https://preprod.dsp5-archi-o24a-g2.fr/grafana
      - GF_SERVER_SERVE_FROM_SUB_PATH=true
    volumes:
      - grafana_data_preprod:/var/lib/grafana
      # Chemins ABSOLUS sur l'hote, et non relatifs.
      #
      # Jenkins lance docker compose depuis /var/jenkins_home/workspace/... qui
      # n'existe que DANS le conteneur Jenkins, alors que le demon docker tourne
      # sur l'hote. Un montage relatif ./monitoring/... est donc resolu sur
      # l'hote, ou ce chemin n'existe pas : docker cree un dossier vide sans
      # erreur, et Grafana demarre sans aucun dashboard.
      #
      # Consequence : le depot de la VM doit etre a jour (git pull) pour que les
      # dashboards et la configuration Prometheus soient ceux du commit deploye.
      - ${VM_PROJECT_DIR:-/home/thetiptop_gp2/furious-duck}/monitoring/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - ${VM_PROJECT_DIR:-/home/thetiptop_gp2/furious-duck}/monitoring/grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
      # Monte hors de /var/lib/grafana : ce chemin est deja occupe par le volume
      # grafana_data_preprod. Doit correspondre a options.path du provider.
      - ${VM_PROJECT_DIR:-/home/thetiptop_gp2/furious-duck}/monitoring/grafana/dashboards:/etc/grafana/dashboards:ro
    networks:
      - default
      - traefik_proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik_proxy"
      - "traefik.http.routers.grafana-preprod.rule=(Host(`preprod.dsp5-archi-o24a-g2.fr`) || Host(`preprod.dsp5-archi-o24a-g2.com`)) && (Path(`/grafana`) || PathPrefix(`/grafana/`))"
      - "traefik.http.routers.grafana-preprod.priority=1000"
      - "traefik.http.routers.grafana-preprod.entrypoints=websecure"
      - "traefik.http.routers.grafana-preprod.tls=true"
      - "traefik.http.routers.grafana-preprod.tls.certresolver=letsencrypt"
      - "traefik.http.services.grafana-preprod.loadbalancer.server.port=3000"
    depends_on:
      - prometheus

  # Metriques systeme de la VM hote : CPU, RAM, disque, reseau, charge.
  # Les points de montage /proc, /sys et / sont montes en lecture seule car
  # node-exporter lit les compteurs du noyau, pas ceux du conteneur.
  node-exporter:
    image: prom/node-exporter:v1.8.2
    container_name: furious_duck_node_exporter_preprod
    restart: unless-stopped
    command:
      - "--path.procfs=/host/proc"
      - "--path.sysfs=/host/sys"
      - "--path.rootfs=/host/root"
      # Sans ce filtre, chaque couche overlay docker apparait comme un systeme
      # de fichiers distinct et pollue les metriques disque.
      - "--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc|rootfs/var/lib/docker)($$|/)"
      - "--collector.netdev.device-exclude=^(veth|docker|br-|lo).*$$"
    pid: host
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/host/root:ro,rslave
    networks:
      - default
    # Pas de label traefik : ces metriques ne doivent pas etre exposees
    # publiquement, Prometheus les scrape via le reseau interne.

  # Metriques par conteneur : CPU, memoire, reseau et I/O de chaque service.
  # Complete node-exporter, qui ne voit que la machine dans son ensemble.
  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.49.1
    container_name: furious_duck_cadvisor_preprod
    restart: unless-stopped
    command:
      # cAdvisor collecte par defaut beaucoup de metriques inutiles ici et
      # consomme du CPU : on ne garde que ce qui sert aux dashboards.
      - "--docker_only=true"
      - "--housekeeping_interval=15s"
      - "--disable_metrics=percpu,sched,tcp,udp,advtcp,process,hugetlb,referenced_memory,cpu_topology,resctrl"
    privileged: true
    devices:
      - /dev/kmsg:/dev/kmsg
    volumes:
      # rslave est indispensable : un bind mount de / ne propage pas les
      # sous-montages de l'hote. Sans lui, /rootfs/var/lib/docker apparait vide
      # et cAdvisor echoue sur chaque conteneur avec
      #   "failed to identify the read-write layer ID ... mount-id: no such file"
      # Il ne remonte alors que le cgroup racine, sans label name, donc aucune
      # metrique par conteneur.
      - /:/rootfs:ro,rslave
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      # Necessaire aux metriques d'I/O disque par conteneur.
      - /dev/disk/:/dev/disk:ro
    networks:
      - default


  # Calcule les quatre metriques DORA a partir de l'API Jenkins et les expose
  # au format Prometheus. Jenkins tourne dans un projet compose separe : on le
  # joint par son nom de conteneur sur le reseau partage traefik_proxy.
  dora-exporter:
    build: ./dora-exporter
    container_name: furious_duck_dora_exporter_preprod
    restart: unless-stopped
    environment:
      # Jenkins est servi derriere --prefix=/jenkins : le prefixe fait partie
      # de l'URL de l'API, sans lui Jenkins repond 404.
      - JENKINS_URL=${DORA_JENKINS_URL:-http://furious_duck_jenkins:8080/jenkins}
      - JENKINS_JOB_PATH=${DORA_JENKINS_JOB_PATH:-job/furious-duck/job/PREPROD}
      - JENKINS_USER=${DORA_JENKINS_USER:-}
      - JENKINS_TOKEN=${DORA_JENKINS_TOKEN:-}
      - DORA_WINDOW_DAYS=${DORA_WINDOW_DAYS:-30}
    networks:
      - default
      - traefik_proxy
    # Pas de label traefik : les metriques restent internes, Prometheus les
    # scrape par le reseau docker.


volumes:
  prometheus_data_preprod:
  grafana_data_preprod:

networks:
  traefik_proxy:
    external: true
