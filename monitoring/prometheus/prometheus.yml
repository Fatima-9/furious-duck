global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    metrics_path: /prometheus/metrics
    static_configs:
      - targets:
          - prometheus:9090

  - job_name: furious-duck-backend-dev
    metrics_path: /metrics
    static_configs:
      - targets:
          - backend:5000
